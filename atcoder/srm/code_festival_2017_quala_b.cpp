#include <bits/stdc++.h>
using namespace std;
#define rep(i, n) for (int i = 0; i < (int)(n); i++)
using ll = long long;
const ll MOD = 1e9 + 7;

int main()
{
    int n, m, k;
    cin >> n >> m >> k;

    for (int nbit = 0; nbit < (1 << n); ++nbit)
    {
        for (int i = 0; i < m; ++i)
        {
            if (int i = 0; i < n; i++)
            {
            }
        }
        for (int mbit = 0; mbit < (1 << m); ++mbit)
        {
        }
    }
}